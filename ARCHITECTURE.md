# 系统架构

> 细节层辅助文档 - 分层架构与技术选型

<!-- 创建于 init-project，后续由 /zn-update-docs 维护 -->

---

## 架构概览

Wenfxl Codex Manager 采用**分布式主从架构**，支持单机部署和多节点集群模式。

```
┌─────────────────────────────────────────────────────────┐
│                    Web Console (Vue.js)                 │
│  ┌──────────┬──────────┬──────────┬──────────────────┐ │
│  │ 任务控制 │ 库存管理 │ 集群控制 │ 实时日志流(SSE)  │ │
│  └──────────┴──────────┴──────────┴──────────────────┘ │
└─────────────────────────────────────────────────────────┘
                           ↓ HTTP/WebSocket
┌─────────────────────────────────────────────────────────┐
│              FastAPI Backend (Python 3.11+)             │
│  ┌──────────────────────────────────────────────────┐  │
│  │  routers/  - API路由层 (认证/任务/库存/集群)     │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │  utils/    - 核心引擎                            │  │
│  │    ├─ config.py         - 配置管理               │  │
│  │    ├─ db_manager.py     - SQLite库存             │  │
│  │    ├─ proxy_manager.py  - 代理池管理             │  │
│  │    ├─ register.py       - 注册引擎               │  │
│  │    ├─ email_providers/  - 邮箱后端(15+ types)    │  │
│  │    └─ integrations/     - 第三方集成             │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│                    外部服务层                            │
│  ┌──────────┬──────────┬──────────┬──────────────────┐ │
│  │ CPA仓库  │ Sub2API  │ HeroSMS  │ Telegram Bot     │ │
│  └──────────┴──────────┴──────────┴──────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

## 层次划分

### 表现层 (Web Console)
- **技术栈:** Vue.js 3 + Vanilla JS
- **职责:** 用户交互、实时状态展示、配置管理UI
- **通信:** HTTP REST API + Server-Sent Events (SSE)

### 路由层 (routers/)
- **技术栈:** FastAPI Router
- **职责:** 请求路由、认证验证、参数校验
- **关键端点:** `/login`, `/start`, `/stop`, `/accounts`, `/cluster/*`

### 业务层 (utils/)
- **技术栈:** Python 3.11+ 异步编程
- **职责:** 核心业务逻辑、任务编排、状态管理
- **核心模块:**
  - `core_engine` - 注册任务主循环
  - `config` - 配置加载与热更新
  - `db_manager` - 本地库存持久化
  - `proxy_manager` - 代理池与节点切换

### 数据层
- **本地存储:** SQLite (账号库存、任务历史)
- **配置存储:** YAML (data/config.yaml)
- **云端仓库:** CPA API / Sub2API (远程库存同步)

---

## 关键架构决策

| 决策 | 选择 | 原因 |
|------|------|------|
| Web框架 | FastAPI | 原生异步支持、自动API文档、高性能 |
| 数据库 | SQLite | 轻量级、无需独立服务、适合单机部署 |
| 前端框架 | Vue.js 3 | 渐进式、易集成、响应式数据绑定 |
| 代理管理 | Clash/Mihomo API | 节点切换灵活、支持多协议、社区成熟 |
| 日志流 | Server-Sent Events | 单向推送、浏览器原生支持、无需WebSocket |
| 集群通信 | WebSocket | 双向通信、低延迟、支持心跳保活 |
| 邮箱后端 | 插件化架构 | 支持15+种后端、统一接口、易扩展 |

---

## 分布式集群架构

### 主从模式
- **主控节点 (Master):** 运行Web Console，接收用户指令
- **工作节点 (Worker):** 执行注册任务，上报日志和账号

### 通信协议
- **心跳:** 每5秒通过WebSocket发送节点状态
- **命令下发:** 主控通过 `/cluster/control` 发送启动/停止指令
- **日志上报:** 工作节点实时推送日志到主控
- **账号上传:** 工作节点完成任务后批量上传账号到主控

### 节点发现
- 工作节点配置 `cluster_master_url` 指向主控地址
- 主控维护 `CLUSTER_NODES` 字典记录在线节点
- 节点离线超过30秒自动从列表移除

---

## 技术选型理由

### 为什么选择FastAPI而非Flask？
- **异步原生支持:** 注册任务需要大量并发I/O（邮箱轮询、代理切换）
- **自动API文档:** 生成OpenAPI规范，便于前端对接
- **类型提示:** Pydantic模型提供运行时校验

### 为什么选择SQLite而非PostgreSQL？
- **部署简单:** 无需独立数据库服务，适合Docker单容器部署
- **性能足够:** 账号库存查询量不大（<10万条），SQLite性能满足需求
- **备份方便:** 单文件数据库，直接复制即可备份

### 为什么使用SSE而非WebSocket推送日志？
- **单向推送:** 日志流只需服务端→客户端，无需双向通信
- **浏览器原生支持:** EventSource API，无需额外库
- **自动重连:** 断线后浏览器自动重连，无需手动处理

---

## 扩展性设计

### 邮箱后端插件化
- 所有provider实现统一接口：`generate_email()`, `get_otp()`
- 新增后端只需在 `email_providers/` 添加新文件
- 配置文件中指定 `email_service` 即可切换

### 代理池水平扩展
- 支持多容器代理池（`warp_proxy_list`）
- 每个worker使用独立代理端口
- 代理失败自动切换到下一个端口

### 集群节点动态扩展
- 新节点只需配置 `cluster_master_url` 即可加入
- 主控自动发现并分配任务
- 节点离线不影响其他节点运行

---

*架构文档 | 生成时间: 2026-04-16*
